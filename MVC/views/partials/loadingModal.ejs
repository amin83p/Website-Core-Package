<div id="globalLoadingModal" class="app-loading-modal" aria-hidden="true">
  <div class="app-loading-shell" role="status" aria-live="polite" aria-atomic="true">
    <div class="app-loading-logo-watermark" aria-hidden="true"></div>
    <div class="app-loading-content">
      <div class="spinner-border text-primary app-loading-spinner" role="status" aria-hidden="true">
        <span class="visually-hidden">Loading...</span>
      </div>
      <h5 id="globalLoadingTitle" class="app-loading-title">Please wait</h5>
      <p id="globalLoadingNote" class="app-loading-note mb-0">Processing your request...</p>
      <p id="globalLoadingOperationWrap" class="app-loading-operation d-none mb-0">
        Operation: <span id="globalLoadingOperation"></span>
      </p>
      <div id="globalLoadingProgressWrap" class="app-loading-progress d-none">
        <div class="progress">
          <div
            id="globalLoadingProgressBar"
            class="progress-bar progress-bar-striped progress-bar-animated"
            role="progressbar"
            aria-valuemin="0"
            aria-valuemax="100"
            aria-valuenow="0"
            style="width: 0%;"
          >0%</div>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
  .app-loading-modal {
    position: fixed;
    inset: 0;
    z-index: 11150;
    display: none;
    align-items: center;
    justify-content: center;
    padding: 1.5rem;
    background:
      radial-gradient(1200px 420px at 80% -30%, rgba(13, 110, 253, 0.22), rgba(13, 110, 253, 0)),
      radial-gradient(900px 360px at -10% 110%, rgba(25, 135, 84, 0.16), rgba(25, 135, 84, 0)),
      rgba(15, 23, 42, 0.22);
    backdrop-filter: blur(3px);
  }

  .app-loading-modal.is-visible {
    display: flex;
  }

  .app-loading-shell {
    position: relative;
    width: min(520px, 92vw);
    border-radius: 22px;
    border: 1px solid rgba(255, 255, 255, 0.42);
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.80), rgba(255, 255, 255, 0.58));
    box-shadow: 0 24px 54px rgba(15, 23, 42, 0.24);
    overflow: hidden;
  }

  .app-loading-shell::before {
    content: "";
    position: absolute;
    inset: 0;
    border-radius: 22px;
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.28);
    pointer-events: none;
  }

  .app-loading-logo-watermark {
    position: absolute;
    top: 14px;
    left: 16px;
    width: 132px;
    height: 86px;
    background-image: var(--app-brand-logo-url, url('/uploads/GLOBAL/logo/Logo1.png'));
    background-repeat: no-repeat;
    background-position: top left;
    background-size: contain;
    opacity: 0.12;
    filter: grayscale(8%);
    pointer-events: none;
  }

  .app-loading-content {
    position: relative;
    z-index: 1;
    padding: 1.65rem 1.45rem 1.35rem;
    text-align: center;
  }

  .app-loading-spinner {
    width: 2.35rem;
    height: 2.35rem;
    border-width: 0.27rem;
    margin-bottom: 0.75rem;
  }

  .app-loading-title {
    margin: 0 0 0.45rem;
    font-weight: 700;
    letter-spacing: 0.01em;
    color: #1f2937;
  }

  .app-loading-note {
    color: #4b5563;
    font-weight: 500;
  }

  .app-loading-operation {
    margin-top: 0.48rem;
    color: #0f4c81;
    font-size: 0.94rem;
    font-weight: 600;
  }

  .app-loading-progress {
    margin-top: 0.85rem;
  }

  .app-loading-progress .progress {
    height: 0.68rem;
    border-radius: 999px;
    background: rgba(15, 23, 42, 0.09);
    overflow: hidden;
  }

  #globalLoadingProgressBar {
    min-width: 1.2rem;
    font-size: 0.65rem;
    line-height: 0.68rem;
    font-weight: 700;
  }

  @media (max-width: 576px) {
    .app-loading-content {
      padding: 1.35rem 1rem 1rem;
    }
    .app-loading-logo-watermark {
      top: 10px;
      left: 12px;
      width: 96px;
      height: 62px;
      opacity: 0.1;
    }
    .app-loading-title {
      font-size: 1.05rem;
    }
    .app-loading-note {
      font-size: 0.93rem;
    }
  }
</style>
